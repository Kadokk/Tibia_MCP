# TibiaEdge Phase 5 — Wiki Corpus, Catalog Grounding & Monetization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The bot's answers about items, creatures, spells, NPCs, and hunting spots become corpus-grounded instead of model-prior guesses, and the product grows a payment path. A weekly TS importer builds a full TibiaWiki catalog in Postgres (**zero LLM calls** — every content type is a structured portable infobox; ~14.9k pages, ~21 min batched); six SQL-backed local tools plus a hard CATALOG prompt rule replace the live-fetch C++ `search_item`/`search_creature`/`search_spell` in the agent loop; and a monetization track lands premium purchase plumbing behind an evaluation-gated payments decision (the VPS is outbound-only — Stripe webhooks are not assumed), with the CipSoft fansite-programme inquiry email drafted first because launch marketing is gated on it. Exit gate for the phase overall: **"one stranger pays"** — a post-merge business gate tracked in the launch checklist, not a merge gate.

**Re-scope note (supersedes the spec's Phase 5 section):** owner decisions 2026-07-19 + 2026-07-21: Phase 5 = corpus + catalog tools + monetization; **insight engine + weekly digest defer to Phase 6** (the spec's `005_insights.sql` renumbers to 007); **Hunting Places are IN** (443 pages, the content type most directly aimed at the known hunting-spot grounding gap); self-hosted TibiaData container recommended DEFER to Phase 6 (nothing in this phase needs it; flag at plan sign-off).

**Architecture:** Three moving parts. (1) **Catalog pipeline** — a shared `wikiApiClient` (extracted from the Phase 4 quest importer, plus `list=embeddedin` enumeration and 50-title batched fetch) feeds `wikiCatalogImporter` (CLI `npm run import:catalog` + weekly scheduler offset from the quest import). Enumeration is by template transclusion (`Infobox_Object`/`_Creature`/`_Spell`/`_NPC`/`_Hunt`); unchanged pages are skipped by per-row `source_revision`; parsing is pure deterministic infobox-param extraction (no LLM anywhere in this pipeline); rows land in migration 005's `catalog_*` tables. Runtime never touches Fandom. (2) **Grounding surface** — six local tools (`get_item_info`, `find_items`, `get_creature_info`, `get_spell_info`, `get_npc_info`, `find_hunting_places`) routed through the Phase 3 tool router (public data, no tier gate), a CATALOG system-prompt rule (never state spawns/loot/stats/prices not returned by a tool), and a tool-list filter in `main.ts` that drops the three redundant C++ search tools from the loop (the C++ tools themselves stay; `/price` is untouched this phase). (3) **Monetization track** — an evaluation spike decides Discord-native App Subscriptions vs Stripe Payment Link + outbound polling vs Stripe webhook (the last only with explicit ops sign-off — outbound-only VPS is a hard invariant); migration 006 `entitlements` + an idempotent tier-sync service map purchases onto the existing `user_tiers` plumbing; `/upgrade` gives every premium gate a CTA; `docs/launch-checklist.md` gates marketing on the CipSoft email.

**Tech Stack:** TypeScript (ESM, `tsx`), discord.js v14, raw `pg` via `DbClient`, OpenRouter via the `openai` SDK (agent loop only — the catalog importer makes zero model calls), vitest. **Zero new npm dependencies** expected; the payments task may justify exactly one (e.g. `stripe`) — decided at Task 16's checkpoint, never earlier.

**Spec:** `docs/superpowers/specs/2026-07-15-tibiaedge-second-brain-design.md` (Phase 5 section, as re-scoped above). Exit criteria:
1. Full catalog import lands live: creatures ≥ 2,800, spells ≥ 215, NPCs ≥ 1,400, hunting places ≥ 440, each row carrying `source_revision`, `wiki_url`, and CC BY-SA attribution. Items: **record the actual post-filter count** (the itemid+pickupable/whitelist filter has never run against the 9,972-object superset); if it lands under ~3,000, investigate the filter before accepting — a ledger ruling, not an automatic failure.
2. Grounding: golden eval green including 6–8 new catalog cases; "where should a level 250 EK hunt" style answers cite catalog rows (hunting places / creature spawn locations), not model-prior spawn names.
3. Payments: decision doc approved by owner; the chosen path implemented and verified in test mode end-to-end (purchase event → `user_tiers` flip → premium gate opens); admin tier never downgraded by sync.
4. `docs/fansite-inquiry.md` and `docs/launch-checklist.md` exist; the email is owner-sent before any launch marketing (checklist enforces).

---

## Working agreements

- **Branch:** `feat/v2-phase5-corpus-monetization` from `main` (f1ee4fa or later), worktree `~/.config/superpowers/worktrees/Tibia-MCP/tibiaedge-phase5`. Never commit to `main`. This plan file is the branch's first commit.
- **Run TS tests from** `services/discord-bot/`: `npx vitest run <file>` for one file, `npx vitest run` for all — **never plain `npm test`** (parks in vitest watch mode and hangs gate runs). Typecheck: `npm run typecheck`. Lint: `npm run lint`. No C++ changes are planned; if a task ends up touching `src/` (repo root), the full `ctest --test-dir build` gate applies.
- **TDD every task:** write the failing test, watch it fail, implement minimally, watch it pass, commit.
- **Repository test convention** (see `src/repositories/memoryRepository.test.ts`): fake `DbClient` = `{ query: vi.fn() }`, assert on SQL substring + exact params. Catalog tables are global (not user-scoped) — no isolation tests needed there. **Migration 006's `entitlements` IS user-scoped: every per-user method's test MUST assert the discord user id appears in the SQL params.**
- **`DbClient` is a `pg.Pool` wrapper — no cross-query transactions.** Multi-row atomic changes (e.g. rebuilding an item's trade offers) must be single statements (data-modifying CTEs), same as `forgetEverything`.
- **Zero-LLM invariant (stop-and-flag):** the catalog importer makes NO model calls. If a parsing problem tempts you toward "just one LLM call per page", STOP and flag — the $0-cost/21-minute import is load-bearing (an LLM-per-page fallback would cost ~$5–7 AND take 9+ days under the $0.70/day spend cap). The quest importer keeps its existing capped LLM path unchanged.
- **Politeness:** batched or not, keep the existing ~2 s inter-request throttle and the identifying User-Agent. Full run ≈ 610 requests ≈ 21 min; weekly incremental = revid batches only.
- **Attribution is a legal requirement, not decoration:** every catalog answer surface (tool results) carries the TibiaWiki link and CC BY-SA notice stored on the row, same as quests.
- **Prose exclusion policy (legal):** import structured infobox facts verbatim (fine under CC BY-SA with attribution — migration 004 precedent). NEVER import `flavortext` or `bestiarytext` (CipSoft in-game copy, not wiki-licensed) and do not import page-body prose (`notes`, `history`, strategy sections). Short factual infobox fields (e.g. spell `effect`) are OK.
- **Static prefix rule (carried from Phase 4):** the tool list + system prompt must remain byte-identical across users, tiers, and requests. Tier gating happens only inside dispatchers; per-user content only via the `userContext` block. (Anthropic cache economics are moot post-OpenRouter; determinism still is not.)
- **Model-controlled inputs are hostile.** Tool args come from an LLM; wikitext comes from the web. Every free-text write is validated/length-capped; catalog lookups are parameterized SQL only.
- **Repo is PUBLIC; VPS is outbound-only.** No secrets, keys, SKU ids, or hostnames in commits. No inbound HTTP endpoint may be added without explicit ops sign-off (Task 16 checkpoint). New env vars must ship with safe defaults so the VPS deploy needs no `.env` change unless the payments decision requires one — that one is ops-coordinated in the same window as the deploy.
- **Live-key steps** (`npm run eval*`) need `OPENROUTER_API_KEY=$(security find-generic-password -s openrouter-tibiaedge-dev -w)` — readable from the Brain pane only (Keychain ACL). Flag these steps for the Brain instead of burning retries. DB-backed live steps (Task 15) use the local smoke DB (`tibiaedge_smoke`, Phase 4 precedent); the production import runs on the VPS via the scheduler after merge/deploy.

---

## File structure

**Create:**

| File | Responsibility |
|---|---|
| `services/discord-bot/db/migrations/005_wiki_catalog.sql` | `catalog_items`, `catalog_creatures`, `catalog_spells`, `catalog_npcs`, `catalog_hunting_places`, `catalog_npc_trade_offers`; `wiki_import_runs.content_type` |
| `services/discord-bot/db/migrations/006_entitlements.sql` | `entitlements` (shape finalized by Task 16's decision) |
| `services/discord-bot/src/importers/wikiApiClient.ts` | Shared MediaWiki client: throttle/retry/UA, `embeddedin` enumeration, 50-title batched revid + content fetch |
| `services/discord-bot/src/importers/catalogWikiParser.ts` | Pure wikitext → typed records: infobox param split, per-type mappers, nested-template stripping |
| `services/discord-bot/src/importers/wikiCatalogImporter.ts` | Enumerate → revid-gate → batch fetch → parse → upsert; per-type run bookkeeping; ZERO model calls |
| `services/discord-bot/src/importers/runCatalogImport.ts` | CLI entry (`npm run import:catalog`, `--type`, `--limit N`) |
| `services/discord-bot/src/importers/itemAliases.ts` | Curated `{canonical, aliases}` seed (rescued from the orphaned `tests/fixtures/items_*.json`), merged into `catalog_items.aliases` at import |
| `services/discord-bot/src/importers/fixtures/catalog_*.api.json` | Committed real MediaWiki API fixtures: one item, one non-item object, one creature (Demon), one spell, one NPC (Rashid), one hunting place, one 3-title batch response |
| `services/discord-bot/src/repositories/catalogRepository.ts` | Per-type upserts, revid maps, alias-aware loose finders, CTE trade-offer rebuild, counts |
| `services/discord-bot/src/scheduler/catalogImportScheduler.ts` | Weekly tick driver (distill/quest scheduler template), offset from quest import |
| `services/discord-bot/src/services/tierSyncService.ts` | Entitlement events/polls → idempotent `user_tiers` upsert; admin never downgraded |
| `services/discord-bot/src/repositories/entitlementRepository.ts` | `entitlements` reads/writes (user-scoped; isolation tests) |
| `services/discord-bot/src/commands/upgradeCommand.ts` | `/upgrade`: tier status + purchase CTA per the payments decision |
| `docs/fansite-inquiry.md` | CipSoft fansite-programme email draft + submission checklist (owner sends) |
| `docs/payments-evaluation.md` | Task 16 decision doc (three options, recommendation, owner decision recorded) |
| `docs/launch-checklist.md` | Marketing go/no-go gates: CipSoft email, attribution audit, leak checks, pricing copy |

Plus a co-located `.test.ts` for every `src/` file above.

**Modify:**

| File | Change |
|---|---|
| `services/discord-bot/src/importers/wikiQuestImporter.ts` | Refactor onto `wikiApiClient` (behavior unchanged; tests stay green) |
| `services/discord-bot/src/repositories/wikiImportRunRepository.ts` | `start(contentType)` + `finish` keep the existing columns (`pages_seen/pages_updated/pages_failed`); `content_type` written per run |
| `services/discord-bot/src/agent/localTools.ts` | Six catalog tool defs + routes (public, before the premium gate — `get_quest_info` pattern) |
| `services/discord-bot/src/agent/systemPrompt.ts` | CATALOG rule (rule 9 — note rule 1 is already named GROUNDING): item/creature/spell/NPC/hunting facts must come from catalog tools; never invent spawn locations, loot, stats, or prices |
| `services/discord-bot/src/main.ts` | Filter `search_item`/`search_creature`/`search_spell` out of the merged tool list (line ~115); wire catalog repo/importer/scheduler; registry deps |
| `services/discord-bot/src/commands/registry.ts` | `/upgrade` wiring; upsell copy in existing premium-gate replies gains the CTA |
| `services/discord-bot/src/config/env.ts` | `CATALOG_IMPORT_TICK_MS` (default 604 800 000), `CATALOG_IMPORT_ENABLED` (default true); payments env per Task 16 (safe defaults) |
| `services/discord-bot/package.json` | `"import:catalog"` script |
| `services/discord-bot/eval/run.ts` + `eval/userFixtures.json` + `eval/toolFixtures.json` + `eval/golden.json` | Catalog local-tool fakes + fixture rows; 6–8 catalog cases + 1–2 gating cases (20 → ~28); maintain golden.json's `_todo` key |
| `docs/beta-deployment-checklist.md` | Phase 5 verification section (append-only) |

**Explicitly unchanged:** all C++ (`src/`, repo root — the C++ search tools keep serving MCP consumers and `/price`, whose direct `mcp.callTool('search_item')` path is unaffected by the loop-list filter; a `/price`-to-catalog follow-up is ledgered), migrations 001–004, `src/agent/agentLoop.ts`, quest importer behavior (refactor only).

---

## Design invariants (load-bearing, read before any task)

1. **Verified corpus scale (live api.php, re-verified 2026-07-21):**

| Enumeration (ns 0) | Mechanism | Count |
|---|---|---|
| `Template:Infobox_Object` | `list=embeddedin` | 9,972 |
| `Template:Infobox_Creature` | embeddedin | 2,843 |
| `Template:Infobox_NPC` | embeddedin | 1,455 |
| `Template:Infobox_Spell` | embeddedin | 218 |
| `Template:Infobox_Hunt` | embeddedin | 443 |
| `Category:Quest Overview Pages` (Phase 4, reference) | categorymembers | 367 |

   Enumerate by **template transclusion only**. `Category:Items` is useless (6 direct pages + overlapping attribute subcats); `Template:Infobox_Item` has 0 transclusions; `Category:Hunting Places` (460) includes the list page and non-infobox pages — `embeddedin` on `Infobox_Hunt` (443) is canonical.

2. **Batched fetch verified live 2026-07-21:** one `action=query&titles=<50 titles>&prop=revisions&rvprop=content|ids&rvslots=main` request returned full content for 50/50 pages, no `continue`. Full run ≈ 610 requests (~21 min at the 2 s throttle) vs ~8 h with Phase 4's one-page-per-request pattern — batching is a hard prerequisite (Task 2 before any importer work). Revid pre-check batches (`rvprop=ids` only) make the weekly incremental cheap.

3. **Item filtering happens at parse time.** The Object superset includes non-items (live batch sample: "Rookgaard", "Fire"). Keep a row as an *item* iff the infobox has `itemid` AND `pickupable = yes`, OR its `objectclass` is in a curated whitelist (weapons/armor/runes/potions/amulets/rings/tools/valuables...). Rows failing the filter are skipped (not stored). Test edge pages: a key, a doll, a rune, "Fire", "Rookgaard". Wrong filter either bloats the table with scenery or drops quest items — fixture-test both directions.

4. **Infobox shapes (all live-verified 2026-07-20/21; capture real fixtures in Task 4–7, do not hand-write them):**
   - **Object/Item** (`{{Infobox Object}}`): `itemid, objectclass, primarytype, slot, levelrequired, vocrequired, attack, defense, armor, weight, npcvalue, npcprice, buyfrom, sellto, value, marketable, pickupable, stackable, actualname, plural`. `sellto`/`buyfrom` are comma-separated NPC lists with optional per-NPC price overrides — grammar: `Name`, or `Name: 110` (e.g. Plate Armor `sellto = H.L.: 110, Rashid: 400`). `value` may be a range.
   - **Creature** (`{{Infobox Creature}}`, Demon fixture): `hp, exp, armor, mitigation, summon, convince, creatureclass, primarytype, bestiaryclass, bestiarylevel, occurrence, spawntype, isboss`; resistances as percent params `physicalDmgMod, earthDmgMod, fireDmgMod, deathDmgMod, energyDmgMod, holyDmgMod, iceDmgMod, hpDrainDmgMod, drownDmgMod, healMod`; `abilities = {{Ability List |{{Melee|0-500}} |{{Ability|Name|range|element|scene={{...}}}} |{{Healing|range=80-250}} |{{Summon|Fire Elemental|1}}}}` — extract name/range/element, **drop `scene=` payloads entirely**; `maxdmg = {{Max Damage|physical=500|fire=250|...}}`; `location` is a wiki-linked prose param → extract link targets as a place-name array; `loot` via `{{Loot Table |{{Loot Item|1-3|Great Mana Potion|common}}...}}` → `{item, amount, rarity}`. `bestiarytext`/`flavortext` = CipSoft copy — NEVER imported.
   - **Spell** (`{{Infobox Spell}}`): `words, mana, levelrequired, cooldown, voc, premium, effect, spellclass/subclass`.
   - **NPC** (`{{Infobox NPC}}`, Rashid fixture): `job, city, location, buysell` — location params can contain nested `{{#switch:...}}` / `{{Mapper Coords|...}}` noise; the parser must strip nested templates deterministically (keep `city`, degrade `location` to plain text or drop).
   - **Hunting place** (`{{Infobox Hunt}}`, Ab'Dendriel Elf Cave fixture): `city, location, vocation, lvlknights, lvlpaladins, lvlmages, skknights, skpaladins, skmages, defknights, defpaladins, defmages, loot, lootstar, exp, expstar, bestloot..bestloot5, map`; the page body's `==Creatures==` section carries `{{CreatureList|type=...|Snake|Elf|...}}` → creature-name array. Per-vocation level recommendations are the grounding payload for "where should a level X <vocation> hunt".

5. **Catalog tables are namespaced `catalog_*`** — migration 001 already owns a legacy `items` table (archived trade-listener alias data). Do not touch it; do not reuse the name. `/price` keeps calling the C++ `search_item` this phase (ledgered follow-up: point it at catalog aliases in Phase 6).

6. **Migration numbering:** 005 = wiki catalog, 006 = entitlements; the spec's `005_insights.sql` becomes 007 in Phase 6.

7. **Migration 005 conventions** follow 004 exactly: `BIGSERIAL` PK, `slug TEXT UNIQUE NOT NULL`, `title TEXT NOT NULL`, JSONB defaults (`'[]'::jsonb` / `'{}'::jsonb`), `wiki_url TEXT`, `attribution TEXT` defaulting to the CC BY-SA notice, `source_revision BIGINT`, `active BOOLEAN DEFAULT TRUE`, `created_at`/`updated_at`, `lower(title)` indexes. Typed columns for the fields the tools filter/sort on; everything else into an `attributes JSONB` residual bag (future-proofing — schema churn stays low). `catalog_npc_trade_offers`: `item_id BIGINT REFERENCES catalog_items(id) ON DELETE CASCADE`, `npc_name TEXT` (name, not FK — item pages reference NPCs without pages), `direction TEXT CHECK (direction IN ('npc_sells','npc_buys'))`, `price INT NULL` (override, else tool falls back to the item's npc price), `UNIQUE (item_id, npc_name, direction)`.

8. **Payments posture:** the VPS accepts no inbound connections. Option (a) Discord App Subscriptions delivers `ENTITLEMENT_*` over the existing gateway websocket (outbound — zero posture change); option (b) Stripe Payment Link + polling correlates via `client_reference_id=<discord user id>` baked into the link URL (minutes of lag acceptable for tier grants); option (c) Stripe webhook requires inbound HTTP = ops sign-off or an external relay. Default recommendation encoded in Task 16: **(a) if eligible, else (b); (c) only with explicit ops sign-off.** Eligibility, payout geography, and revenue cut MUST be verified live in the spike, not assumed.

9. **Entitlement rows are billing records, not memories:** `/memory forget everything` does NOT delete `entitlements` (retention is a legal/accounting need and rows contain no conversational content). Document this in the forget-all copy review in Task 17.

---

### Task 1: CipSoft fansite-programme inquiry email (non-code, owner-gated)

**Files:** Create `docs/fansite-inquiry.md`.

Draft the inquiry email per the current fansite-programme rules at cipsoft.com/tibia (verify the current submission channel live): who we are, what TibiaEdge does (AI assistant Discord bot; TibiaWiki CC BY-SA-attributed data; no client interaction, no botting, no packet reading), the freemium model (~$4.99/mo), and the explicit question whether paid features around Tibia-derived public data are acceptable to the programme. Include a submission checklist (channel, date sent, response) the launch checklist will reference.

- [x] **Step 1:** Verify the current fansite-programme contact/submission mechanism on cipsoft.com (live).
- [x] **Step 2:** Write `docs/fansite-inquiry.md` (email body ≤ 400 words, EN; checklist section).
- [ ] **Step 3:** Flag to the owner for send — the send itself is an owner action recorded in the checklist.

**Exit:** doc exists; owner notified. **Commit:** `docs: CipSoft fansite-programme inquiry draft (Task 1)`

### Task 2: `wikiApiClient` — shared client with enumeration + 50-title batching

**Files:** Create `src/importers/wikiApiClient.ts` (+ test). Modify `src/importers/wikiQuestImporter.ts` onto it (its tests stay green unchanged).

Extract the quest importer's fetch/throttle/retry/UA into a shared client and add: `enumerateTransclusions(template)` (`list=embeddedin`, `einamespace=0`, `eilimit=500`, follows `eicontinue`), `fetchRevids(titles[])` and `fetchContent(titles[])` (both chunk into ≤50-title `action=query&prop=revisions` batches, `rvslots=main`, follow `continue` defensively, preserve throttle between requests). Fixture: one real 3-title batch response (capture live, commit as `catalog_batch.api.json`). **Constraint:** the quest importer's tests inject `http` + `sleep` deps directly — keep those constructor deps and build the `wikiApiClient` internally from them, so `wikiQuestImporter.test.ts` stays byte-untouched (that is the regression net; an injected-client refactor would force test changes and void the exit criterion).

- [x] **Step 1:** Failing tests: enumeration pagination, 50-title chunking, batch response mapping (title → {revid, content}), throttle called between requests, HTTP ≥400 → typed error.
- [x] **Step 2:** Implement; refactor `wikiQuestImporter` to consume the client (no behavior change — its existing tests are the regression net).
- [x] **Step 3:** `npx vitest run src/importers` green; typecheck/lint green.

**Exit:** quest importer tests untouched and green; client fully unit-tested. **Commit:** `refactor(importers): shared wikiApiClient with embeddedin enumeration + 50-title batching (Task 2)`

### Task 3: Migration 005 — wiki catalog schema

**Files:** Create `db/migrations/005_wiki_catalog.sql`. Test via the migration-runner convention used for 004.

Tables per Design invariants 5–7: `catalog_items` (typed: `game_item_id, object_class, primary_type, slot, level_required, vocation, weight NUMERIC, attack, defense, armor, npc_buy_price, npc_sell_price, market_value_low, market_value_high, marketable, stackable, pickupable, actual_name, plural, aliases JSONB, attributes JSONB` + standard columns; indexes: `lower(title)`, `lower(actual_name)`, GIN on `aliases`, `(object_class, level_required)`), `catalog_creatures` (`hp, exp, armor, mitigation, bestiary_class, bestiary_level, occurrence, is_boss, creature_class, primary_type, spawn_type, summon_cost, convince_cost, abilities JSONB, resistances JSONB, max_damage JSONB, loot JSONB, locations JSONB, attributes JSONB`; index `(exp)`), `catalog_spells` (`words, spell_class, subclass, vocations JSONB, level_required, mana, premium, cooldown NUMERIC, effect TEXT, attributes JSONB`; index `lower(words)`), `catalog_npcs` (`job, city, location TEXT, buysell BOOLEAN, attributes JSONB`; index `lower(city)`), `catalog_hunting_places` (`city, location TEXT, vocations TEXT, level_knights, level_paladins, level_mages, loot_rating, loot_stars, exp_rating, exp_stars, best_loot JSONB, creatures JSONB, attributes JSONB`; index `(level_knights)`), `catalog_npc_trade_offers` (per invariant 7), and `ALTER TABLE wiki_import_runs ADD COLUMN content_type TEXT NOT NULL DEFAULT 'quest'`.

- [x] **Step 1:** Write the SQL.
- [x] **Step 2:** Manual verification (Phase 4 style — unit tests stay fake-DbClient-only, never live-Postgres): apply 001→005 against a fresh `tibiaedge_smoke`; assert key tables/columns/indexes/constraints via `psql \d`.
- [x] **Step 3:** Typecheck/lint green (no TS in this task; vitest suite unaffected).

**Exit:** fresh-DB apply of 001→005 verified on the smoke DB. **Commit:** `feat(db): migration 005 wiki catalog tables (Task 3)`

### Task 4: Parser — infobox split + object/item mapper

**Files:** Create `src/importers/catalogWikiParser.ts` (+ test). Commit fixtures `catalog_item.api.json` (a real armor/weapon page) and `catalog_object_nonitem.api.json` (e.g. "Fire" or "Rookgaard").

Core: `parseInfoboxParams(templateName, wikitext)` — brace-depth-aware param splitter (params contain nested `{{...}}`; naive `|` splitting breaks on Demon's abilities). Item mapper: filter per invariant 3; `buyfrom`/`sellto` grammar with per-NPC `Name: price` overrides → trade-offer records; `value` range parsing; numeric coercion tolerant of commas and `?`; residual params → `attributes`.

- [x] **Step 1:** Failing tests: param split on nested templates; filter accepts the item fixture and rejects the non-item; trade-offer grammar incl. override and no-override forms; value range; comma numbers.
- [x] **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** both fixtures parse to expected typed records. **Commit:** `feat(importers): infobox parser + item mapper with objectclass/pickupable filter (Task 4)`

### Task 5: Parser — creature mapper

**Files:** Extend `catalogWikiParser.ts` (+ test). Commit fixture `catalog_creature_demon.api.json` (real Demon page).

Loot Table → `{item, amount, rarity}[]`; Ability List → `{name, range?, element?}[]` with `scene=` dropped; `{{Max Damage|...}}` → typed map; the ten `*DmgMod`/`healMod` percent params → `resistances` map (strip `%`, keep int); `location` param → wiki-link targets array; `bestiarytext`/`flavortext` explicitly excluded (test asserts absence).

- [x] **Step 1:** Failing tests against the Demon fixture (known values: hp 8200, exp 6000, fireDmgMod 0%, ability "Great Fireball" 150–250 fire, loot rows, prose exclusion). **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** Demon parses to the full expected record. **Commit:** `feat(importers): creature mapper — loot, abilities, resistances, locations (Task 5)`

### Task 6: Parser — spell + NPC mappers

**Files:** Extend `catalogWikiParser.ts` (+ test). Commit fixtures `catalog_spell.api.json` (e.g. Ultimate Healing), `catalog_npc_rashid.api.json` (real Rashid page — worst-case nested-template noise).

Spell mapper: `words/mana/level/cooldown/voc/premium/effect`. NPC mapper: `job/city/buysell`; `location` degraded to plain text with nested templates (`{{#switch:...}}`, `{{Mapper Coords|...}}`) stripped deterministically.

- [x] **Step 1:** Failing tests (Rashid: city survives, no template braces leak into stored text). **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** both fixtures parse clean. **Commit:** `feat(importers): spell + NPC mappers with nested-template stripping (Task 6)`

### Task 7: Parser — hunting-place mapper

**Files:** Extend `catalogWikiParser.ts` (+ test). Commit fixture `catalog_hunt.api.json` (real Ab'Dendriel Elf Cave page).

Infobox Hunt params per invariant 4 (per-vocation levels, ratings + stars, bestloot1–5 → array); body `==Creatures==` `{{CreatureList|...}}` → creature names (skip `type=` param); `location` → plain text (Mapper Coords stripped).

- [x] **Step 1:** Failing tests (lvlknights 20, lvlmages 25, lootstar 2, bestloot "Wand of Cosmic Energy", creatures [Snake, Elf, Elf Scout, Elf Arcanist]). **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** fixture parses to the full expected record. **Commit:** `feat(importers): hunting-place mapper (Task 7)`

### Task 8: `catalogRepository`

**Files:** Create `src/repositories/catalogRepository.ts` (+ test).

Per-type `upsert*` (ON CONFLICT slug, refresh `source_revision`/`updated_at`), `getRevisionMap(contentType)` (slug → revid for the incremental gate), loose finders: `findItemLoose(q)` (exact ci title → `actual_name` → alias GIN containment → prefix), `findCreatureLoose`, `findSpellLoose` (title OR `words`), `findNpcLoose`, `findItems(filters)` (class/slot/max level/vocation/sort/limit), `findHuntingPlaces({level, vocation, limit})` (per-vocation level column ≤ level, order by rating), `rebuildTradeOffersForItem(itemId, offers[])` as ONE data-modifying CTE (delete + insert), `counts()`.

- [x] **Step 1:** Failing tests: SQL substring + params per convention; CTE is a single `query` call; alias lookup hits GIN containment syntax. **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** all repo methods unit-tested. **Commit:** `feat(repos): catalogRepository — upserts, loose finders, CTE trade-offer rebuild (Task 8)`

### Task 9: Item alias seed

**Files:** Create `src/importers/itemAliases.ts` (+ test). DELETE the orphaned untracked `tests/fixtures/items_test.json`, `items_regex_test.json`, `items_llm_test.json` from the MAIN checkout (their content is adopted here — note in the commit body).

Curate the `{canonical, aliases}` seed (MSW→Magic Sword, SD→Sudden Death Rune, TC→Tibia Coin, UH, GFB, BoH, ...; start from the orphaned fixtures' entries, extend with the common-abbreviation set). Importer merges seed aliases into `catalog_items.aliases` (union, deduped, lowercased) when the canonical title matches. **Note:** the fixture deletion is a cross-checkout step — the orphans are untracked files in the MAIN checkout (`/Users/kadokk/AI-Devs/Kadokk/Tibia-MCP`), not this worktree, so the deletion itself cannot be committed; record it in the commit body and do it out-of-band.

- [x] **Step 1:** Failing tests: seed shape valid; merge unions without duplicates; unknown canonicals logged, not fatal. **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** alias seed live in the import path. **Commit:** `feat(importers): curated item alias seed (adopts orphaned fixtures) (Task 9)`

### Task 10: `wikiCatalogImporter`

**Files:** Create `src/importers/wikiCatalogImporter.ts` (+ test). Modify `src/repositories/wikiImportRunRepository.ts` (+ its test): `start(contentType)` writes the migration-005 `content_type` column (current `start()` takes zero args); `finish` keeps the existing columns.

Per content type (`item|creature|spell|npc|hunt`): enumerate via `wikiApiClient` → batch-fetch revids → diff against `getRevisionMap` → batch-fetch content for changed/new slugs only → parse → upsert (+ trade-offer rebuild for items; alias merge per Task 9) → `wiki_import_runs` row per run with `content_type` and the EXISTING columns (`pages_seen` = enumerated, `pages_updated` = upserted, `pages_failed` = parse failures; skipped is derivable, no new columns; `llm_cost_usd_micros` always 0 here). Zero model calls (test asserts the AI client is never constructed/injected). Per-page parse failures are logged and counted, never abort the run.

- [x] **Step 1:** Failing tests: revid gate skips unchanged; changed-only content fetch; per-type bookkeeping incl. `content_type` in `start`'s SQL params; failure isolation. **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** importer fully unit-tested against fixtures. **Commit:** `feat(importers): wikiCatalogImporter — zero-LLM batched corpus pipeline (Task 10)`

### Task 11: CLI + scheduler + env

**Files:** Create `src/importers/runCatalogImport.ts`, `src/scheduler/catalogImportScheduler.ts` (+ tests). Modify `src/config/env.ts`, `src/main.ts`, `package.json`.

`npm run import:catalog -- [--type item|creature|spell|npc|hunt] [--limit N]` (Phase 4 CLI conventions: dummy Discord/MCP env vars documented in the file header). Weekly scheduler on the quest-scheduler template — note that template kicks at boot and resets its timer every restart, so a "+24 h first tick" would never fire on a frequently-deployed bot; instead kick at boot like the quest scheduler but with a fixed ~10-minute initial delay (staggers the two importers' fetch windows; post-first-import a boot kick is nearly free thanks to the revid gate). Env: `CATALOG_IMPORT_TICK_MS` default 604 800 000, `CATALOG_IMPORT_ENABLED` default true — **safe defaults, no VPS `.env` change needed**.

- [x] **Step 1:** Failing tests: env parsing/defaults; scheduler tick calls importer; CLI arg handling. **Step 2:** Implement + wire in `main.ts`. **Step 3:** Gates green.

**Exit:** `npm run import:catalog -- --type spell --limit 3` runs live against the smoke DB (3 spells land). **Commit:** `feat(importers): catalog CLI + weekly scheduler + env knobs (Task 11)`

### Task 12: Catalog local tools

**Files:** Modify `src/agent/localTools.ts` (+ test) AND `src/main.ts` — the six tools add `catalog` to `LocalToolDeps`/`createToolRouter`, and `main.ts`'s `createToolRouter({...})` call site must pass the new dep in the SAME task or the typecheck gate fails (Phase 4 deferred wiring bit; Phase 5's per-task gates don't allow it).

Six defs + routes (public data — registered BEFORE the premium gate, exactly like `get_quest_info`): `get_item_info(item)` (loose lookup → stats, requirements, npc buy/sell + which NPCs w/ per-NPC overrides, market value range, wiki link + attribution), `find_items(object_class?, slot?, max_level?, vocation?, sort_by?, limit≤10)`, `get_creature_info(creature)` (stats, resistances, abilities, loot w/ rarity, **spawn locations**, attribution), `get_spell_info(spell)` (name or incantation), `get_npc_info(npc)` (job, city, trades inverted from `catalog_npc_trade_offers`, capped), `find_hunting_places(level, vocation, limit≤5)` (per-vocation level filter, rating-ordered, creatures + best loot per row, attribution). Not-found → helpful "not in catalog" string (the model falls back to `search_wiki`).

- [x] **Step 1:** Failing tests: schemas, dispatch, tier-independence (tool list byte-identical across tiers), attribution present in every result, caps enforced. **Step 2:** Implement. **Step 3:** Gates green.

**Exit:** router exposes 6 new tools, all tiers. **Commit:** `feat(agent): six SQL-backed catalog tools (Task 12)`

### Task 13: CATALOG rule + tool-list filter + eval fakes

**Files:** Modify `src/agent/systemPrompt.ts`, `src/main.ts`, `eval/run.ts`, `eval/userFixtures.json`, `eval/toolFixtures.json` (+ tests).

System prompt gains rule 9, named **CATALOG** (rule 1 is already named GROUNDING and covers "every fact from a tool result"; rule 9's distinct payload is tool *preference* + honest misses): item/creature/spell/NPC/hunting-place questions MUST go through the catalog tools; if the catalog has no row, say so or fall back to `search_wiki` — never fill the gap from memory. `main.ts` filters `search_item`, `search_creature`, `search_spell` out of the merged tool list (named exclusion set with a comment; `search_wiki`/`search_quest` stay). Eval harness gains catalog local-tool fakes backed by small fixture rows.

- [x] **Step 1:** Failing tests: prompt contains the rule; filtered list excludes exactly the three; eval fakes return fixture rows. **Step 2:** Implement. **Step 3:** Gates green (full `npx vitest run` — the tool-list change touches many snapshots).

**Exit:** loop tool list = MCP minus 3 plus 6 local. **Commit:** `feat(agent): CATALOG prompt rule + redundant C++ search tools filtered from loop (Task 13)`

### Task 14: Golden eval growth — catalog cases

**Files:** Modify `eval/golden.json` (+ `eval/userFixtures.json` as needed).

6–8 new cases (20 → 26–28): item stat lookup by alias ("what does an MSW do"), best-gear query (`find_items`), creature loot + resistances ("what's immune to fire"), spell by incantation, NPC trade ("who buys plate armor and for how much"), hunting recommendation for a linked char's level/vocation (asserts a catalog place name, not a model-prior name), one negative case (obscure thing not in catalog → honest fallback). Content-word langMarkers only (Phase 3 lesson — function-word markers flake).

- [x] **Step 1:** Add cases; run the eval against fakes locally (no live key needed for structure). **Step 2:** [BRAIN] Live run: `npm run eval` with the dev OpenRouter key — target green; soft warns triaged.

**Exit:** live eval ≥ 26 cases green — achieved as 28/28, ZERO hard failures, on the shipping default `anthropic/claude-haiku-4.5` (owner-approved model flip, see ledger). Full output archived by Brain (`eval_record_attempt2.txt`). **Commit:** `test(eval): 6-8 catalog grounding cases (Task 14)`

### Task 15: Live full import + verification [BRAIN/live]

**Files:** Modify `docs/beta-deployment-checklist.md` (append Phase 5 verification section).

Against the local smoke DB: `--limit 20` dry-run per type first, then the full run. Assert exit-criterion counts; spot-check Magic Sword (aliases include "msw"), Demon (fire resistance 0%, spawn locations non-empty), Rashid (city present, no `{{` leakage), Ab'Dendriel Elf Cave (lvlknights 20). Record wall-clock and request count in the checklist section. Re-run immediately → near-total revid skip (incremental gate proof).

- [x] **Step 1:** Dry runs. **Step 2:** Full run + assertions. **Step 3:** Incremental re-run proof. **Step 4:** Checklist section appended.

**Exit:** exit-criterion 1 numbers recorded. **Commit:** `docs(checklist): Phase 5 verification — live catalog import results (Task 15)`

### Task 16: Payments evaluation spike (decision task — owner-gated checkpoint)

**Files:** Create `docs/payments-evaluation.md`.

Evaluate live per invariant 8: (a) Discord App Subscriptions — verify current eligibility requirements, supported payout geography (owner is MX-based), revenue cut, entitlement event flow + test-mode story; (b) Stripe Payment Link + outbound polling — verify `client_reference_id` round-trip in test mode, polling API costs/limits, lag; (c) Stripe webhook — enumerate exactly what ops would need to expose. **[BRAIN/OWNER] Any test-mode credentials (Stripe test keys, Discord dev-portal monetization setup) are owner/Brain-held — flag those steps like the eval live-key steps, never burn retries.** Write the doc with a filled comparison table and the default recommendation ((a) if eligible else (b)). **STOP: cmux notify — owner decision required before Tasks 17–18. Approval integrity (standing Phase 4 rule): the decision counts ONLY as an explicitly submitted owner message or a verbatim `[Brain] OWNER APPROVED: <decision>` relay — composer text and dialog selections are not approval.** Record the decision in the doc.

**Exit:** owner decision recorded. **Commit:** `docs: payments evaluation + decision (Task 16)`

### Task 17: Migration 006 + entitlements + tier sync

**Files:** Create `db/migrations/006_entitlements.sql`, `src/repositories/entitlementRepository.ts`, `src/services/tierSyncService.ts` (+ tests). Modify `src/main.ts` (wiring per decision: gateway event handler or polling scheduler), `src/config/env.ts` (payments env, safe defaults; any required VPS `.env` addition documented for the ops handoff).

`entitlements`: `id BIGSERIAL, provider TEXT, external_id TEXT, discord_user_id TEXT, sku TEXT, status TEXT, raw JSONB, created_at, updated_at, UNIQUE(provider, external_id)`. Tier sync: active entitlement → `user_tiers` upsert to `pro`; expired/revoked → back to `free`; **admin/disabled rows never modified**; idempotent (replayed events are no-ops). Isolation tests per Working agreements. Forget-all copy notes billing-record retention (invariant 9).

- [x] **Step 1:** Failing tests (incl. isolation + admin-never-downgraded + idempotency). **Step 2:** Implement per the Task 16 decision. **Step 4:** Gates green.
- [ ] **Step 3 [BRAIN/OWNER]:** End-to-end in test mode: purchase event/poll → tier flips → premium gate opens; revoke → flips back. Needs real Stripe test-mode credentials (owner/Brain-held) — not attempted by Coder, everything else built and unit-tested against fakes and verified live against real Postgres.

**Exit:** test-mode purchase round-trip verified. **Commit:** `feat(payments): entitlements + idempotent tier sync (Task 17)`

### Task 18: `/upgrade` + upsell CTAs

**Files:** Create `src/commands/upgradeCommand.ts` (+ test). Modify `src/commands/registry.ts` (wiring) plus the files that actually own the premium-gate copy: `src/commands/goalsCommand.ts`, `src/commands/questCommand.ts`, `src/commands/askCommand.ts`, `src/commands/linkCommand.ts`, and `src/agent/localTools.ts` (`PREMIUM_MEMORY_MESSAGE`). Modify `eval/golden.json` (1–2 gating cases).

`/upgrade`: current tier, what pro adds, the purchase CTA per the decision (SKU button or payment link — **no SKU ids/links hardcoded in the repo; env-injected**). Every enumerated upsell surface gains the same CTA line. Ephemeral replies.

- [x] **Step 1:** Failing tests. **Step 2:** Implement. **Step 3:** Gating eval cases pass. **Step 4:** Gates green.

**Exit:** every premium wall points at `/upgrade`. **Commit:** `feat(commands): /upgrade + unified premium CTAs (Task 18)`

### Task 19: Launch checklist

**Files:** Create `docs/launch-checklist.md`.

Gates, in order: CipSoft email SENT (date + channel; response posture recorded — no marketing before send, marketing tone adjusted to any response), attribution audit (every catalog/quest surface shows CC BY-SA + link — scripted spot-check list), cross-user-leak verification steps (isolation eval cases + manual two-account drill), pricing copy review, rollout-week metrics baseline (DAU/questions/spend from §2 Step 5), "one stranger pays" tracking (who/when/plan).

**Exit:** doc exists and references Task 1's inquiry doc. **Commit:** `docs: launch checklist — marketing gated on CipSoft inquiry (Task 19)`

### Task 20: Final gates + version bump

**Files:** Modify `services/discord-bot/package.json` (version 0.1.0 → 0.3.0 — the file was never bumped for v0.2.0-beta, so the jump is deliberate; note it in the commit body), `README.md` (roadmap: Phase 5 shipped, Phase 6 = insights + vault export + TibiaData container decision), `docs/beta-deployment-checklist.md` (final Phase 5 sign-off block).

- [x] **Step 1:** Full `npx vitest run` + typecheck + lint green.
- [ ] **Step 2:** [BRAIN] Live golden eval (~28–30 cases) green on `anthropic/claude-haiku-4.5` (new code default, owner-approved 2026-07-21 — see ledger); Qwen re-evaluation deferred to a beta-week experiment ticket, not a merge-gate target.
- [x] **Step 3:** README/version/checklist updates.
- [ ] **Step 4:** STOP — human gates: push/PR approval, then merge approval. **Approval integrity (standing Phase 4 rule): only an explicitly submitted owner message or a verbatim `[Brain] OWNER APPROVED: <what>` relay counts — composer text and dialog selections are not approval.** Post-merge deploy note for ops: catalog env vars have safe defaults (no `.env` change); payments env per Task 17's handoff block; first VPS catalog import runs via scheduler or manual CLI in the deploy window.

**Exit:** all merge-gate evidence collected. **Commit:** `chore: v0.3.0 — Phase 5 corpus grounding + monetization (Task 20)`

---

## Out of scope (Phase 6+)

- Insight engine + weekly digest (spec Phase 5 → deferred; migration renumbered to 007).
- `/export vault`, reply-to-continue, full multi-language eval expansion (spec Phase 6).
- Self-hosted TibiaData container (recommended defer — confirm at plan sign-off).
- `/price` migration onto catalog aliases; retiring the C++ search tools outright.
- Embeddings / full-text semantic search over the catalog; DISTILL_SYSTEM Qwen tuning (separate beta-week ticket).
- Any inbound HTTP endpoint beyond what Task 16's decision explicitly approves with ops.

## Ledger

*(Execution session appends dated entries here: plan-vs-reality conflicts, Brain rulings, deferred follow-ups.)*

- **2026-07-21, Task 2:** unplanned addition, orchestrator-accepted (no owner escalation needed — implementation detail, no scope/invariant impact). Coder found live that `api.php` returns `query.normalized` (e.g. `Light_Healing` → `Light Healing`) and batch lookups key on title, so an unnormalized title would silently miss its page. Added `aliasNormalized` to map results back to the requested spelling. Relevant because Task 10's revid-diff enumeration would otherwise see a phantom "everything changed" run. Caveat noted by Coder: two `fetchPageContent` tests assert title-mismatch tolerance using deliberately mismatched fixtures (by construction, to prove the single-page-return behavior) — this is intentional test design, not a coverage gap in the new client; the pre-existing (byte-untouched) `wikiQuestImporter.test.ts` suite doesn't separately re-verify normalization on the `/Spoiler` fetch path specifically, which is a pre-existing minor gap, not a regression.
- **2026-07-21, Task 3, Brain ruling:** `wiki_url` on all `catalog_*` tables is `TEXT NOT NULL`, matching migration 004 — invariant 7's column list omitting `NOT NULL` was shorthand; the intent stated in invariant 7 ("follow 004 exactly") governs. Reasoning: `wiki_url` is always derivable from the enumerated title, and it feeds the CC BY-SA attribution surface, a legal requirement on every catalog answer — nullable would invite silently broken attribution. Applied before the Task 3 commit.
- **2026-07-21, Task 5, deferred follow-up for Task 10:** `embeddedin` on `Infobox_Creature` is a superset like Objects — 16 of 60 sampled pages transcluded the template indirectly (hunting places, `Loot/*` subpages, geography, Main Page) and carry no creature infobox. `mapCreature` returns `null` for these; Task 10's importer must treat a `null` map result as "skip, not a parse failure" (do not count these toward `pages_failed`).
- **2026-07-21, Task 6, same follow-up for NPCs:** `embeddedin` on `Infobox_NPC` is the same superset class — 52/60 sampled pages had a real NPC infobox, 8 were indirect transclusions with none. Same rule for Task 10: `mapNpc` returning `null` is a skip, not a `pages_failed` count. Also two real defects found and fixed test-first: Levitate's `<br />`-joined alternative incantations, and `animation = {{Scene|...}}` template markup leaking into the spell attributes bag — the residual-bag guard (drop any value still containing `{{`) is now unified across item/creature/spell mappers.
- **2026-07-21, Task 7, note for Task 10:** unlike Items/Creatures/NPCs, `Infobox_Hunt` enumeration showed **no superset effect** — 60/60 sampled pages mapped cleanly, so Task 10's hunt import path does not need the skip-not-fail handling the other three content types require. Also confirmed live: no sampled page carries a `bestloot6` or beyond, so the plan's five-slot assumption holds. All 5 parser mappers (item/creature/spell/npc/hunt) now complete, ~290 real pages sampled total across Tasks 4–7, zero markup leaks.
- **2026-07-21, Task 8:** `rebuildTradeOffersForItem`'s CTE prunes only the complement of the incoming offer set (not a delete-all) — a same-statement delete-all would expose those rows to the INSERT's `ON CONFLICT` uniqueness check under the same snapshot, which Postgres documents as unpredictable. The method also de-duplicates repeated `(npc_name, direction)` pairs defensively (Postgres rejects an `ON CONFLICT DO UPDATE` payload that repeats a constrained key), rather than trusting `parseTradeList`'s current behavior. Both hand-verified against real Postgres in a rolled-back transaction, beyond the plan's fake-DbClient-only requirement, given the hazard.
- **2026-07-21, Task 9:** orphaned fixtures adopted and de-duplicated (they disagreed on casing, "MSW" vs "msw"). Checking all 33 seed canonicals against the live wiki caught 4 mismatches: "Soft Boots" and "Tibia Coin" are redirects (corrected to "Pair of Soft Boots" / "Tibia Coins", human spelling kept as an alias); "Ultimate Healing Rune" and "Intense Healing Rune" are spell-infobox pages, with the actual item on the separate "(Item)" page. Cross-checkout fixture deletion done out-of-band by the Orchestrator (2026-07-21): confirmed untracked, unreferenced by any code, then `rm`'d from `/Users/kadokk/AI-Devs/Kadokk/Tibia-MCP/tests/fixtures/` (items_test.json, items_regex_test.json, items_llm_test.json). Not a git operation in the main checkout (files were never tracked), so nothing to commit there.
- **2026-07-21, Task 10, Brain ruling — file-scope extension:** Task 10 hit a real gap Coder flagged via dialog: Task 9's alias seed had no write path, since Task 8's `upsertItem` deliberately omits the `aliases` column. Brain ruled option 1 (add `mergeItemAliases(title, aliases)` to `catalogRepository` + its tests as a 3rd file in this task, one UPDATE unioning seed aliases into the existing JSONB array) over deferring to a new task (would retroactively void Task 9's committed exit criterion "alias seed live in the import path") or having the importer write its own SQL (breaks the repository-owns-SQL architecture invariant). Per-task file lists are guides; invariants are the contract. Coder's own dialog framing and Task 8's file are hereby extended by this one method for Task 10.
- **2026-07-21, Task 12 follow-up (orchestrator-directed, interstitial):** rendering real smoke-DB rows through the new catalog tools surfaced a real defect — raw `[[link]]` wiki markup was not stripped to display text in creature ability/loot/location values (only `{{template}}` markup had a unified guard, from Task 6). Directed as a small fix before Task 14's eval work, so eval cases don't bake in dirty strings: strip `[[...]]` to display text across the affected mappers, extend the no-markup test to assert `[[` as well as `{{`, and add a `--force` CLI flag to backfill existing dirty rows (the revid gate means a parser fix alone does not heal already-imported rows). Separately, Coder flagged a lower-severity, in-spec limitation (incidental `[[level]]` link in creature location prose extracted as a place name) — left alone per its own Task 5 assessment, not a new defect. **Resolved (5aad618):** Task 6's "no markup reaches the database" claim was only ever tested for `{{`, never `[[` — the claim matched its own verification and missed its intent. Fixed systematically (every typed field now degrades through `stripToPlainText`, covering all five mappers at once) rather than patching creature abilities alone. Verified firsthand: `--force` backfill against the smoke DB, zero remaining `[[` across all 4 populated catalog tables (items/creatures/npcs/hunts) confirmed via direct SQL scan.
- **2026-07-21, Task 14 follow-up (orchestrator-directed, interstitial): trade-offer reader gap.** The plan's own Task 12 spec named `get_npc_info` showing "trades inverted from `catalog_npc_trade_offers`," but no code reads that table — a populated table with no reader, corroborated by Task 3's `idx_catalog_npc_trade_offers_npc_lower` index (flagged speculative at the time) sitting unused. Not a correctness bug or data loss — Plate Armor's `sellto = H.L.: 110, Rashid: 400` parses and stores correctly, it's just unreachable through any tool. Directed as a small additive interstitial fix (repository finders + enrich `get_item_info`/`get_npc_info` rendering, no new tool, no prompt change) so the eval can later carry the NPC-trade case as originally specified instead of a substitute. Scoped explicitly to exclude `eval/` files: Brain's Task 14 Step 2 live eval run was in progress against the currently committed golden.json at the time this was directed, and changing eval fixtures mid-run would move the target. The "who buys plate armor" eval case is a fast follow-up once Brain's run completes. **Resolved (d61f30b):** `findTradeOffersForItem`/`findTradeOffersForNpc` added, both tools now render offers (capped at 6 per direction, best-first, NULL price meaning "trades at the item's default price" rather than free). Bonus fix: `level_required = 0` no longer renders as a false "Requires: level 0" constraint. **Known follow-up, not yet done:** `eval/run.ts`'s catalog fake is cast `as never` and does not yet implement the two new finders — deliberately left broken-at-runtime rather than touched, since Brain's eval was mid-run against the committed golden.json. Must be fixed before the next eval run.
- **2026-07-21, Brain verdict — Task 14 Step 2 live eval NOT green (8/27 hard failures, $0.0572):** Class A (4 cases) — model never called the required catalog tool (item-alias, find-items, spell, hunt cases; creature+npc passed) despite the CATALOG rule. Class B — `en-catalog-miss-1` content failure at ~42k tokens, possible tool-call loop. Class C (regression signal) — `en-refusal-1`, `es-refusal-1`, `pl-knowledge-1` all failed, all green at the pre-Phase-5 migration gate; two refusal cases failing together suggests the new prompt/tool-list shifted behavior, not flake. Directed to Coder for root-cause diagnosis via systematic-debugging before any fix, covering all three classes plus wiring the eval fake for the trade-offer finders. Brain re-runs the live eval on request once fixes land; Brain is proceeding with Task 15 (full import) in parallel, independent of the eval outcome. **Root-caused and fixed (6698083):** Class B was an eval-harness defect, not a model or prompt problem — the fake ignored tool arguments and keyed answers by tool name only, so any `search_wiki` call returned the canned Dragon article regardless of query; the honest-miss case got confidently wrong data, retried, and hit `MAX_ROUNDS`. Fixed via `_subjects` in toolFixtures.json (a canned answer only stands in for its declared subject) plus bounding rule 9's fallback to one `search_wiki` attempt. Class C: rule 9 had no scope limit, so any question merely mentioning a creature pulled in a catalog lookup — fatal for refusal cases (refusePass fails on any 3+ digit number, and a dragon lookup supplies 1000 hp) and for the Polish case (relayed English source data instead of a native-language answer). Fixed: rule 9 now defers to rule 4 (refusals), excludes broad "what is X" questions, and states the answer must be in the user's language. Class A: proven NOT a weak-tool-description problem (the same `get_item_info` tool was both skipped and called correctly across different cases in the same batch) — root cause is parametric knowledge overriding an instruction that read as a preference; the four failing cases ask for facts the model is confident it already knows. Fixed: rule 9 now makes the catalog call mandatory before stating any specific stat/price/recommendation, explicitly even when the model feels certain. Bonus: closed an evidence gap in the eval harness itself (report now shows which tools ran, round count, and answer head on every hard failure — this diagnosis would have been guesswork without it), and a structural gap (`eval/` was covered by neither `typecheck` nor `lint`, letting the catalog fake drift from `LocalToolDeps` behind an `as never` cast; new `tsconfig.check.json` closes it, verified live by intentionally breaking the fake and confirming `tsc` catches it). 28 cases now, `en-catalog-npc-trade-1` added for the trade-offer reader. Re-run relayed to Brain.
- **2026-07-21, Brain verdict — eval rounds 2-3 + Haiku A/B, model-default question now owner-level.** Post-fix re-runs on `qwen/qwen3.6-flash`: 9/28 then 7/28, with a SHIFTING failure set — 6 stable (hunt no-tools, miss step-cap loop, find-items, en-refusal, pt-auction, pl-knowledge) and 4 that flip run-to-run, meaning genuine variance makes a 28-green gate on Qwen statistically unreachable, not a matter of one more prompt tweak. `anthropic/claude-haiku-4.5`-via-OpenRouter A/B: 27/28, only `en-catalog-find-items-1` failed — the SAME case fails on both models, proving it is a model-independent bug in the case/tool/harness, not a prompt or model-capability issue. This is the documented env-flip fallback trigger from the original OpenRouter migration plan (`AI_MODEL` was built as a reversible knob exactly for this scenario — see `docs/superpowers/plans/2026-07-20-openrouter-migration.md` line ~147). Directed: Coder investigates `find-items-1` as a model-independent bug via systematic-debugging; all further Qwen-specific prompt hardening is PAUSED; the Qwen-vs-Haiku default-model decision now sits with the owner (cost/reliability tradeoff), not something to engineer around. Task 16 continues in parallel, unaffected.
- **2026-07-21, composer-ghost-text near-miss (Task 16 gate):** Coder's composer briefly showed `[Brain] OWNER APPROVED: option (b) Stripe P...` — a dimmed autocomplete suggestion, not real input; Coder was actually idle awaiting instruction ("tell me what to write"), and no genuine owner sign-off had occurred. Per the standing approval-integrity rule, not acted on; overwritten with the real find-items-1 directive instead. Task 16's decision table remains unfilled pending an explicit owner message or verbatim Brain relay.
- **2026-07-21, Brain — Task 15 live full import DONE, exit criteria recalibrated.** Live counts: items 6564 (1 fail, see bug below), creatures 2193, spells 218/218, npcs 1245, hunts 443/443, trade_offers 11837. All spot-checks pass, zero markup leaks. **Ruling:** the plan's exit-criterion 1 numbers (creatures ≥2800, NPCs ≥1400) were set from raw `embeddedin` transclusion counts (2843, 1455) without accounting for the superset skip rate Tasks 5–6 later identified empirically (creature sample ~27% non-creature, NPC sample ~13% non-NPC). Brain sample-verified 193/500 "missing" creature pages in the first batch are legitimate non-creature transclusions (Rookgaard, Carlin, Hero Cave — city/dungeon pages). Real counts of 2193/1245 are consistent with the known skip rate, not an import defect — the original targets were miscalibrated, not missed. **Bug directed to Coder (interstitial):** item "Naga Nest" fails with a Postgres integer overflow (`value 3956839569 out of range for integer`) — some `catalog_items` column is `INT` but this wiki value exceeds the int4 range (~2.147B). Fix: widen the column to `BIGINT` (amend migration 005 directly, since it is unreleased — no separate ALTER migration needed), then a targeted re-import for the affected item(s). Brain is running the incremental re-run proof now; checklist append + Task 15 commit follow once that completes.
- **2026-07-21, `en-catalog-find-items-1` root cause (e53a9e6), a genuine production bug found via cross-model eval evidence.** Same case failed on both Qwen and Haiku, ruling out a prompt issue. Two independent, compounding defects: (1) **Production** — `find_items`'s tool schema declared `required: ['search']` even though every filter is independently optional in the repository; a pure browse-by-category question (no name fragment) forced the model to invent one, which then hit a title `ILIKE` match no title could ever satisfy (an object class is not a title substring). Measured against the smoke DB: querying "other items" returned 0 rows while that class holds 2,212 real rows — the tool's core discovery use case was silently broken since Task 12. Fixed: every filter now optional, empty search omitted rather than passed as `''` (which reached SQL as `ILIKE '%%'`), and the empty-result message names what was actually searched so the model can self-correct. (2) **Harness** — the eval fake matched only three hardcoded `search` substrings and had no `object_class`/`slot`/`max_level` handling at all, so it returned `[]` for exactly the filter-only shape that was broken in production — a false negative that burned two eval rounds pointing suspicion at the prompt. Fixed to mirror repository semantics. `golden.json` deliberately unchanged — Coder correctly declined to "fix" the fake alone, which would have gone green while leaving the real bug live. Verified firsthand: full suite green, and the 2,212-row figure confirmed directly against the smoke DB.
- **2026-07-21, [Brain] OWNER APPROVED: default model flips to `anthropic/claude-haiku-4.5`.** Implementation: the code default (`env.ts` zod default for `AI_MODEL`) changes, not the VPS `.env` — no ops coordination needed, since `AI_MODEL` was already designed as an env-overridable knob. Task 20's merge-gate live eval now targets the new default. Qwen re-evaluation becomes a beta-week experiment ticket alongside the existing `DISTILL_SYSTEM` ticket, not a blocker. Rule 9's scope fixes (refusal/broad-question exclusion, language-match requirement, mandatory-call wording) stay as-is — they serve Haiku too, not just Qwen. Sequencing: bundled with the Naga Nest `BIGINT` fix and the `find-items-1` model-independent bug into one interstitial batch; Brain re-runs the eval on the new default once that lands. **Implemented (3cb705e).** Flipped via a new exported `DEFAULT_AI_MODEL` constant rather than an inline zod default: Coder found `eval/run.ts` and `eval/distill.ts` each hardcoded their own Qwen fallback for when `AI_MODEL` is unset, separate from `env.ts`'s default — flipping only the zod default would have left the eval harness silently grading Qwen while production shipped Haiku, exactly the kind of drift `eval/`'s new typecheck coverage is meant to prevent. Both harnesses now import the shared constant. Test-fixture literals that pass an explicit model to a constructor (incl. the byte-untouched `wikiQuestImporter.test.ts`) are deliberately left naming Qwen, since they don't exercise the default.
- **2026-07-21, Naga Nest bug — Brain's suggested `BIGINT` fix investigated and NOT applied, with evidence.** Root cause was not a real oversized wiki value: Naga Nest's infobox declares `itemid = 39568,39569` (two client ids on one page), and `coerceInt`'s comma-stripping (needed for real thousand-separators like "1,200") fused the unspaced list into the fabricated number 3956839569. Coder determined widening `game_item_id` to `BIGINT` would not have fixed this — it would have silently stored the fabricated id permanently in a column future features join on, turning a loud page-scoped error into silent data corruption — and found no legitimate need for the width (160 sampled item pages across the full 9,972-page corpus: zero real values exceed int4 range; real Tibia item ids are 5 digits). **Fixed instead (5053cf0):** `mapItem` splits the field and keeps the first id; `coerceInt` itself is unchanged. Verified firsthand: Naga Nest and 4 other comma-list items upsert cleanly with `game_item_id` still genuinely `integer` (not widened), and the original fabricated value still overflows int4 when inserted directly, confirming the fix addresses the real defect rather than masking it. **Deferred, low priority:** Coder floated widening `market_value_low`/`market_value_high` to `BIGINT` instead, since an extreme market value is at least conceivable unlike a billion-valued item id — declined for now, no overflow observed in the 160-sample scan; noted here in case a future import run hits one.
- **2026-07-21, Brain — record eval on the new default (Haiku): 27/28, $0.35.** Sole failure still `en-catalog-find-items-1`, but the failure shape changed with the `find_items` fix: rather than the tool being misused, Haiku now asks a clarifying question (level? vocation?) instead of calling `find_items` at all — "tools called: (none)". Directed: amend rule 9 (CATALOG) with a browse-then-refine pattern — for list/browse questions, call `find_items` with whatever filters are already known and show results before asking for refinements. The eval case is NOT to be weakened; it correctly encodes the intended product behavior (show something useful first, refine after), the prompt needs to catch up to it. **Cosmetic, lower priority:** the eval harness's progress line prints `✓ en-catalog-find-items-1 (1 rounds)` (checkmark = "round completed") while the results table correctly scores it FAIL — the checkmark reads as a pass indicator during quick triage and is misleading; worth a follow-up display fix, not blocking.
- **2026-07-21, Task 15 atomicity follow-up, resolved (577ed71).** Brain's "partial row" finding was worse than cosmetic: `upsertItem` persisted the new `source_revision` in its own committed statement, and if the separate `rebuildTradeOffersForItem` call then threw, the page counted as failed but the revid gate would see the page as unchanged on every future run — permanently skipping it. Coder reproduced this against real Postgres before fixing it (measured: revision advances, offers frozen at the stale value, next run confirmed stuck). Fixed with one combined CTE (`upsertItemWithTradeOffers`, mirroring Task 8's shape) so Postgres provides the only atomicity primitive `DbClient` has, since it exposes no cross-call transactions. The old two-statement methods were deleted outright rather than left available, with a test asserting no non-atomic path remains on the repository. Verified firsthand: full suite 617/617 green, the CTE reviewed directly, the delete-old-methods guard test confirmed present, and the real CLI pipeline (`--force --type item`) independently confirmed populating `source_revision` correctly end-to-end. (Naga Nest's row showed a stale NULL `source_revision` from Coder's earlier ad-hoc verification script for the id-parsing fix, predating this commit — not a defect in the atomicity fix; a subsequent real import call over it populated it correctly.)
- **2026-07-21, three findings surfaced during atomicity verification, triaged (corrected per Brain).** (1) `--force` clears `source_revision` for the WHOLE content type, not scoped to `--limit`, silently turning the next run into a near-full refetch — the Orchestrator's own `--force --type item --limit 20` verification (and an earlier Coder test) reset all ~6,557 item rows' revision markers on the smoke DB. Not data loss (titles/stats/offers intact, confirmed), self-heals on the next full item run. Brain's framing: worth a CLI guard/warning later, not a blocking fix — softer than the Orchestrator's initial "fix it now" directive; Coder's implementation approach should follow whichever reads cleaner once it gets there. (2) **Corrected:** the "mystery" single failing page in Brain's production-scale runs (22, 27) was NOT a second, unrelated bug — Brain confirmed from preserved stderr that both runs show the identical Naga Nest int-overflow line, and both runs predate 5053cf0. No second failing page exists; the initial "unrelated to Naga Nest" framing was wrong, based on not having the run timestamps. (3) Observability gap (persist failing titles per run, not just a stderr log) — Brain says keep it ledgered regardless, it's real and worth doing later. Smoke-DB revision reset acknowledged by Brain as fine: Task 15's record is the committed checklist (0424b49), not live DB state — no re-run needed.
- **2026-07-21, browse-then-refine rule-9 amendment, resolved (53fccda).** Fixed the second record-eval failure: Haiku asked clarifying questions (level? vocation?) instead of calling `find_items` for browse-style questions. Rule 9 now says to call `find_items` immediately with whatever filters the question and PLAYER NOTES already provide, show results, and offer to narrow afterwards — never ask for criteria first when the catalog can already answer. A sequencing fix, not a new obligation: the tool-call requirement already existed, and `find_items` has accepted filter-only queries since `e53a9e6`. `golden.json`'s case deliberately untouched — it was correct, and is what caught this. Verified: full suite 623/623 green (once the bundled `--force` work below also landed), typecheck/lint clean.
- **2026-07-21, `--force`/`--limit` footgun, resolved (c0d1338) — better than either suggested fix.** Rather than scoping the pre-clear to the limited pages (the Orchestrator's ask) or just adding a warning (Brain's softer framing), Coder redesigned `--force` to not mutate anything before the run at all: it's now a per-run property that skips the revid comparison for in-scope pages, so a forced run touches nothing it doesn't also re-import. `clearSourceRevisions` deleted along with the destructive pattern it existed to serve — no footgun left for a future caller. Verified firsthand against the smoke DB: 218 spell revisions intact before and after `--type spell --limit 3 --force`, matching Coder's claim exactly.
- **2026-07-21, Brain — final eval verdict 25/28, NOT green; one more fix round directed.** (1) `en-catalog-find-items-1` unchanged despite the browse-then-refine amendment (still zero tools, clarifying questions) — directed to first verify the amended rule actually reaches the built prompt the eval uses before assuming the fix took effect; if it does reach the eval and is still ignored, move the browse-first mandate into `find_items`'s tool description itself, which binds harder than a system-prompt rule. (2) `pl-knowledge-1` fails — zero tools, hallucinated Polish-language Tibia facts — likely slipping through rule 9's broad-question carve-out; directed to tighten it (a broad "what is X" should still ground when X is a real catalog entity) and separately check the case's own assertions for brittleness. (3) `en-memory-write-1`, first-ever failure — `remember` was called correctly, but the reply was too generic for the case's content assertions; directed to review the assertions per the Phase 3 content-marker precedent before touching model behavior, since the underlying tool-use behavior is likely already correct. Brain's plan: this is the last fix round before up to 3 more record attempts, gate = one clean 28/28. **Resolved (cf23434), all three verified against evidence before changing anything.** (1) Confirmed by capturing a real request through the eval's own path (not assumed): the amended rule-9 text genuinely reaches the model (all four amended phrases present, `find_items` correctly advertised) — the model simply ignores one line inside an 11,124-character system message. Fixed by moving the browse-first mandate into `find_items`'s own tool description (read at tool-selection time), leaving the eval case untouched. (2) Rule 9's broad-question carve-out narrowed to exempt only non-catalog subjects (game concepts/mechanics), not real catalog entities like "dragon." Separately, `pl-knowledge-1`'s own assertion was found to be toothless — no `mustContain`, no `mustCallTool`, its only gate was `langPass` (≥2 of 3 Polish function-word markers), which a correct answer need not satisfy either way; broadening markers alone would have gone green while still hallucinating. Now also asserts `mustCallTool: get_creature_info` — the case tests grounding for the first time, a strengthening not a weakening. (3) `en-memory-write-1`'s `remember` call was already correct; its langMarkers required echoing "weekend mornings" verbatim, which a valid terse confirmation ("Noted.") doesn't. Markers broadened to words any correct English memory-write confirmation would carry; `mustCallTool: remember` (the real assertion) untouched. Verified firsthand: full suite 626/626 green, typecheck/lint clean, `langPass`'s `>=2` marker threshold confirmed in `eval/run.ts` so the broadened marker sets remain meaningful, not trivially-true. Relayed to Brain for the next record attempt.
- **2026-07-21, Brain — record attempt 1: 25/28.** Trace-level diagnosis: (a) `en-catalog-miss-1` behavior is now CORRECT (`get_item_info` → one `search_wiki` → an honest "couldn't find" answer) — the case's markers just don't match correct honest phrasing; assertion fix only, no behavior change. (b) `en-catalog-npc-1` — right tools, right answer, failed a DIFFERENT assertion column than the previous run — an assertion-brittleness signal, not a behavior bug. (c) `en-catalog-find-items-1` — a byte-similar zero-tool answer across 4 runs and 3 independent fixes now, too deterministic to be normal prompt-following variance. **Hypothesis:** the eval path may never show the model the actually-updated `find_items` tool description — a stale/duplicate tool-definition copy in the harness, the same class of bug as the `DEFAULT_AI_MODEL` trap Coder caught earlier. Coder's earlier verification captured the SYSTEM PROMPT text reaching the eval, but tool descriptions travel in a separate part of the API request (the `tools` array), which may not have been checked. Directed: capture this case's actual outbound request via the eval path and diff its `find_items` description against `localTools.ts` source, byte for byte, before any further behavior work.
**Resolved (9bac8b2) — the actual root cause was never a tool-def problem.** Coder captured the real outbound `tools` array through the eval path exactly as directed: `find_items`'s description was BYTE-IDENTICAL to `localTools.ts` (861 chars, browse-first mandate included) — the hypothesis was disproven with hard evidence, not argued away. The real cause: TIBIA DOMAIN NOTES elsewhere in the prompt said to "always tailor hunting and gear advice to vocation and level together"; `en-catalog-find-items-1` carries no character fixture, so the model was DETERMINISTICALLY following that competing instruction — the only way to comply with "always tailor to X" when X is unknown is to ask for X. This explains why 4 runs across 3 independent rule-9 fixes produced byte-similar answers: it was never about rule 9's wording. Proven with an existing natural experiment already in the suite: `en-catalog-hunt-1` asks the same question shape but carries a character fixture (`knight25-antica`) and passes — same rule, same model, only difference is whether the demanded facts exist. Fixed by rephrasing both domain-notes instances to tailor when details are known and look-up-and-show first when they are not. (a) and (b) were pure assertion fixes as diagnosed, no behavior touched. Verified firsthand: full suite 629/629 green, typecheck/lint clean, diffs reviewed directly and match the described logic exactly.
- **2026-07-21, THE EVAL SAGA — Task 14, resolved: 28/28, zero hard failures ($0.37), gate GREEN on the shipping default.** Consolidated summary of a long debugging arc (detailed rounds above): `qwen/qwen3.6-flash` scored 19–21/28 across three rounds with a shifting failure set — real variance, not one more prompt tweak away from green — triggering the plan's own documented `AI_MODEL` env-flip contingency. A Haiku A/B (27/28) isolated exactly one model-independent bug, giving the owner a clean signal to flip the default to `anthropic/claude-haiku-4.5`. From there, three more rounds fixed: a harness bug (fake ignored tool args, keyed answers by name only), a real production bug (`find_items` forced a browse question to invent a search string), two brittle/toothless eval assertions (one gained a grounding check it never had), and — the actual root cause of the last holdout — a competing "always tailor to X" instruction in the domain notes that forced clarifying-question behavior whenever character data was absent, proven via a natural experiment already in the suite (an identically-shaped case with a character fixture passed). **Durable lesson:** an "always tailor advice to X" style instruction is a trap when X can be legitimately unknown — write it conditionally ("tailor when known, look up and show first when not") from the start, rather than as an unconditional mandate that silently forces a clarifying-question branch. Every fix in this arc was verified against hard evidence (captured requests, direct DB queries, existing natural experiments) before being applied — no blind prompt tweaking at any point.
- **2026-07-21, Task 19, done (7f07c41), run in parallel with Task 16's owner decision.** `docs/launch-checklist.md`: 6 gates (CipSoft-inquiry-sent, attribution audit, cross-user-leak, pricing copy, rollout metrics baseline, one-stranger-pays exit gate), none of them a merge gate. Real gap found and honestly documented rather than papered over: **the golden eval has zero isolation cases across all 28** — the plan's Task 19 framing assumed they existed. The manual two-account drill is currently the sole end-to-end leak verification; ticketed for a future isolation eval case. Also flagged `/price`'s attribution status as unverified (it still calls the C++ `search_item`, not the catalog) rather than assumed clean. Docs-only task, no code gate applies. **Follow-up (52a1296):** Coder checked the code path directly and confirmed this as a REAL licence-compliance gap, not a maybe — `/price` relays wiki-derived item stats and per-NPC prices verbatim with zero CC BY-SA attribution or wiki link. Correctly not fixed here (moving `/price` onto the catalog is Phase 6 scope) but three remediation options laid out (append a one-line notice now / accept while `/price` stays friendly-tester-only / disable until Phase 6) — recorded as an owner judgement call, does not block merge.
- **2026-07-21, [Brain] OWNER APPROVED: Task 16 payments rail = option (b), Stripe Payment Link + outbound polling.** Relayed verbatim, gate cleared. `docs/payments-evaluation.md`'s Decision section filled per the standing approval-integrity rule. Two conditions attached: (1) Task 17 must implement genuinely two-stage polling — session polling detects signup, a separate subscription-status poll detects renewal/lapse/refund and revokes accordingly; a one-stage build that grants but never revokes is stop-and-flag territory. (2) Added Gate 1b to `docs/launch-checklist.md`: a written inquiry to Discord developer support about the monetization-requirements policy collision (Mexico-ineligible for Premium Apps, but the policy may require offering the paid tier through it) — alongside the existing CipSoft email gate, blocking launch marketing, not Task 17's code. Tasks 17–18 unblocked; sequence confirmed T17 → T18 → T20.
- **2026-07-21, Brain ruling on the `/price` attribution gap: fix now, in-phase.** Option 1 — append the CC BY-SA notice line in `priceCommand.ts` as a tiny interstitial (one line + test; C++ untouched; the Phase 6 catalog migration is unaffected). Rationale: the owner's locked monetization policy already decides this — "completely legal" is a hard requirement, the plan's working agreements call attribution a legal requirement, and Gate 1a's CipSoft email claims attribution on every answer; shipping a known live gap under that claim is not defensible scope discipline, it's just deferred risk. To be slotted in wherever it doesn't disrupt Task 17's flow (Task 17 in progress at time of ruling) — queued as the next quick interstitial once Task 17 lands. Launch-checklist gaps row and Gate 2 row 8 to be updated to resolved-by-&lt;commit&gt; once done.
- **2026-07-21, Task 17 done (b276b4d), verified firsthand against real code and real Postgres.** Genuinely two-stage polling per the owner's condition: `syncSignups` polls Checkout Sessions (the only place `client_reference_id` → discord user id lives) and grants nothing — reaching the success page isn't proof of a completed charge; `syncSubscriptions` polls stored subscription ids for status (the only place renewal/lapse/cancel/refund shows up) and grants on active, revokes on terminal states. Tier writes are SQL-guarded, not application-guarded: `grantProFromEntitlement`'s `ON CONFLICT ... WHERE tier = 'free'` and `revokeProFromEntitlement`'s `WHERE tier = 'pro'` — confirmed directly in `userTierRepository.ts` — so a lapsed subscription cannot touch admin, disabled, or `guild_pro` rows (the last wasn't named in the plan; Coder extended the invariant's spirit correctly). Idempotency confirmed directly: `markStatus`'s `IS DISTINCT FROM` guard, entitlement upsert keyed on `(provider, external_id)`, and a live-verified second identical poll cycle left `updated_at` unchanged. Two edge cases handled: a re-subscribing user on the same subscription id re-arms `revoked` → `pending` without touching an already-`active` row; an unrecognized Stripe status maps to `pending`, never `active`. Zero new npm dependencies — a small `fetch`-based Stripe client built at the edge instead. `PAYMENTS_ENABLED` defaults false, scheduler refuses to start with the switch on but no key (confirmed in `main.ts`) — no VPS `.env` change needed to stay as-is. Forget-all copy updated per invariant 9. Live Postgres verification caught a real bug unit tests couldn't (a shell-mangled `$1` placeholder); a new placeholder-count assertion (confirmed present) closes that class of gap going forward. Full suite 677/677 green, typecheck/lint clean, migration 006 applied and inspected directly on the smoke DB. Step 3 (real Stripe test-mode round trip) correctly left to Brain/owner — everything else is built and unit-tested against fakes.
- **2026-07-21, `/price` attribution gap resolved (d379ebe), per Brain's ruling.** `priceCommand.ts` now appends the same source-link-plus-notice footer the catalog tools emit, built from the item name `search_item` actually resolved (not the player's raw query, avoiding a guessed/404-prone URL). Footer length reserved before Discord-limit truncation, so long results don't push it off the end. No notice on error/not-found, matching the catalog convention (nothing relayed, nothing to attribute). The notice string is duplicated from migration 005's default rather than read from a row (no catalog row exists for `/price` yet) — a test reads the actual migration file and asserts the two strings match, so they cannot silently drift apart before the Phase 6 migration retires this footer. `docs/launch-checklist.md`'s Gate 2 row 8 and known-gaps table updated to resolved. Verified firsthand: full suite 683/683 green, typecheck/lint clean, anti-drift test confirmed reading the real migration file.
- **2026-07-21, Task 18 done (2aac9d9), verified firsthand.** `/upgrade` reads benefits from `getTierLimits` and the price from one constant, so copy cannot drift from enforced limits. Five premium walls (goals, quest tracking cap, daily AI cap, linked-character cap, memory gate) unified to point at `/upgrade`. The critical wiring to Task 17: the purchase URL carries `client_reference_id` set to the caller's Discord id — the only place that identity link exists for Stage 1 of the tier sync to read. `purchaseUrl()` returns `null` (no link shown) rather than risk an unattributable payment if the URL is missing, non-https, malformed, or the discord id wouldn't survive as a reference — confirmed directly in the source, matching the "an unattributable payment is worse than a missing button" design note. No SKU ids or purchase URLs hardcoded in the repo (public); confirmed a real test asserts the source contains no stripe URL, `price_`, or `sku_` pattern. Non-upsellable tiers (pro/guild_pro/admin/disabled) get appropriate messaging instead of a checkout link. Full suite 697/697 green, typecheck/lint clean.
- **2026-07-21, Task 20 Steps 1+3 done (cb736df), verified firsthand.** Version bumped 0.1.0 → 0.3.0 (0.2.0 deliberately skipped, matching the never-bumped-for-beta history). README roadmap and status figures corrected (a stale "336 tests" claim against an actual 697). Beta-deployment-checklist Phase 5 sign-off block added, and — good catch — Coder proactively corrected 3 entries in the Task 15 record that were accurate when written but stale now (Naga Nest, atomicity, `/price` attribution all since fixed). Verified firsthand: full suite 697/697 green, typecheck/lint clean (package.json version confirmed 0.3.0 in the tool output header). Step 2 (Brain's live eval) still pending; Step 4 (push/PR/merge) is the untouched human gate. Coder's context hit 91% at this natural boundary — left a handoff memory note and `/clear`'d for a fresh session before the next task.
- **2026-07-21, Brain — Task 20 Step 2 attempt 1: 27/29 ($0.39).** Both new gating cases and all catalog cases pass. Two failures, both diagnosed as assertion brittleness with CORRECT model behavior in the traces: (1) `en-refusal-1` — a perfect refusal, likely failing a `mustNotContain` check that catches the refusal naming the banned activity itself; (2) `en-memory-write-1` — `remember` called correctly, natural confirmation still under the marker threshold even after this case's markers were already broadened once earlier today. Assertion fixes only, no behavior changes, per the Phase 3 precedent. Dispatched to a fresh Coder session (post-context-reset). **Resolved (995ec68).** `en-refusal-1` had two independent latent bugs, both found: the `mustNotContain` substring check for "cavebot script" also fired on the plural "cavebot scripts" — exactly the phrasing rule 4's own refusal instruction produces when it tells the model to name what it refuses — replaced with markers only real leaked instructions would carry (`step 1`/`step 2`, a code fence), matching the convention the es/pt/pl sibling cases already used. Separately, its `langMarkers` required 2-of-3 hits on `["with","that","not"]`, which correct refusals routinely miss (e.g. "can't" ≠ "not"); broadened to actual refusal vocabulary. `en-memory-write-1`'s markers broadened again to real acknowledgement vocabulary, deliberately excluding ultra-generic stopwords that would make the check trivially true. Verification exceeded what was asked: the assertion logic was replicated offline and both failures reproduced before any edit, then the new markers were checked against a corpus of correct answers AND genuine leaks/wrong-language replies (17 expectations) to confirm the fix doesn't also neuter what the case is supposed to catch. Independently re-verified: realistic English refusals score 7–9 hits (threshold 2), a genuine leak still trips `mustNotContain`, and a Spanish refusal still scores 0. Full suite 697/697 green, typecheck/lint clean.
- **2026-07-21, Brain — Task 20 Step 2 attempt 2: 27/29, a REAL Task 18 design regression found.** The two assertion fixes held (refusal + memory-write now pass). But both gating cases regressed with the same shape — zero tools called, the model explains the premium gate itself instead of calling `remember`/`recall_memory` and letting the dispatcher respond. Brain's diagnosis: something Task 18 added teaches the model about the tier gate BEFORE the call, making the model the gatekeeper — a direct violation of the plan's static-prefix invariant ("tier gating happens only inside dispatchers; per-user content only via the userContext block"). Directed: find what teaches the model pre-call and remove it — tool descriptions must stay gate-neutral, the dispatcher alone decides and returns the CTA-carrying gate message, the model only relays it. `mustCallTool` on both gating cases must NOT be weakened — they caught a real regression by doing exactly their job. This is Brain's last attempt in the current budget (attempt 3 of 3), so the fix must be evidence-verified (a captured pre-call request diff), not a guess.
**Resolved (c7a46b9) — Brain's own causal attribution disproven with hard evidence; the real root cause predates Task 18 entirely.** Coder proved, rather than assumed, that Task 18 was NOT the cause: `2aac9d9` (Task 18) is an ancestor of `f2842a0` (the commit attempt 1 ran green on) — independently confirmed via `git merge-base --is-ancestor` and an empty `git diff --stat` on `src/` between them — so the exact same application code passed once and failed once; nothing in Task 18 could be the trigger. The actual root cause dates to an old commit (`148c312`, pre-dating this phase): rule 7 said "if a memory tool replies that it is a premium feature, relay that briefly" — naming the gate concept without mandating the call, so a model that already "knew" the wall existed could satisfy the rule by asserting it directly, without ever calling the dispatcher. This made the outcome a coin flip on sampling, not a deterministic bug — explaining why identical code passed once and failed once instead of failing every time. Fixed in two tier-neutral places: rule 7 no longer names "premium feature" at all, instead mandating the call whenever the trigger applies and forbidding self-announcement; the same mandate is echoed in the `remember`/`recall_memory` tool descriptions themselves (the `cf23434` find_items lesson: descriptions bind harder than prompt text at tool-selection time). Four new regression tests lock the invariant going forward: rule 7 and both tool descriptions must mandate the call AND must carry zero premium/upgrade/free-tier vocabulary. Verified firsthand: the ancestry and empty-diff claims reproduced myself; rule 7 and tool-description diffs reviewed directly and match the described fix exactly; full suite 701/701 green, typecheck/lint clean.
- **2026-07-21, Brain — Task 20 Step 2 attempt 3: 28/29, gating fixed, one residual attractor left.** The rule-7 treatment held (both gating cases pass). Sole remaining fail: `en-catalog-find-items-1` regressed to ask-first in miniature — a residual ~1-in-5 attractor the domain-notes fix (`9bac8b2`) reduced but didn't eliminate. Brain is deliberately not re-rolling for luck. Directed: apply the exact `c7a46b9` treatment (mandate in both rule 9 and `find_items`'s tool description, plus a locking regression test) to this case — when filter facts are missing, call with whatever is known, show results first, never ask before showing. Explicitly framed as an honest limitation: this is a stochastic failure, so a future green run won't *prove* the fix, only that the mandate is structurally present in both binding surfaces. Brain's next run is framed as fix-then-verify, not a reroll — all 3 attempts each caught a genuinely distinct real defect (2 brittle assertions, 1 latent rule-7 gap, now this).
**Resolved (5a0b109) — a sharper diagnosis than "repeat the mandate louder."** Coder correctly recognized that `find_items`'s description already carried strong, explicit mandate language and still failed, so repeating it a third time wouldn't move the needle. The actual defect: rule 9's browse mandate ended in a CONDITION — "never ask for criteria first when the catalog can already answer with what you have" — which the model can satisfy while still asking, by judging a lone object class insufficient to "answer with." This is the identical structural anti-pattern rule 7 carried before `c7a46b9` (a mandate with a built-in sufficiency judgment call is a preference, not a mandate) — recognized as the same class of bug and fixed the same way: rule 9 now states a partial filter set is *always* enough and a missing level/vocation/PLAYER-NOTES block is never a reason to ask first, with the escape-hatch phrase removed entirely; the same unconditional line is echoed in `find_items`'s description. Three new tests lock it, including a direct negative-regression guard asserting the old escape-hatch phrase is now absent. Honestly framed throughout: this is a ~1-in-5 stochastic failure, so no local run proves the fix — verified structurally via a captured real outbound request confirming the escape hatch is gone from the wire while both mandates are present. Verified firsthand: full suite 704/704 green, typecheck/lint clean, both diffs and all 3 new tests reviewed directly and match the description exactly.
- **2026-07-21, Brain — final run, mustCallTool REVERSAL on the gating cases (structural ruling).** `find_items`'s fix held (passed). But the gating pair and `en-catalog-miss-1` failed with PRODUCT-CORRECT behavior in all three traces. Brain reversed its own earlier standing instruction ("do not drop mustCallTool from the gating cases") based on new evidence accumulated across the 3-attempt whack-a-mole: the eval fixtures carry `tier:free` in `userContext`, which the model legitimately sees — it can always correctly infer the tier gate without calling the memory tools at all, which is efficient correct behavior given real information, not a bug. `mustCallTool` on the gating pair was therefore asserting an unwinnable conversational path, while the actual gate enforcement is already server-side and already unit-tested (Tasks 12/17/18). Replaced with content assertions: premium/upgrade notice present, personalization retained, and — the real risk worth catching — no false claim of persistent storage (a free-tier user must never be told something was saved when the dispatcher didn't store it). `en-catalog-miss-1`: "That item isn't in the catalog" is itself the honest-miss phrasing; markers aligned to that family. Eval-only changes, no prompt/product edits — the verification run after this checks assertions against already-known-correct behavior, not a re-roll of the model. Dispatched to Coder.
**Partially executed (864ba2a) — Coder found Brain's reversal premise factually false and declined that part, with independent evidence.** Two of three directed changes landed: `en-catalog-miss-1`'s markers realigned to the honest-miss phrasing family (the terse correct answer scored exactly 1 marker before), and a false-persistence guard added to both gating cases (apostrophe-agnostic, catching "I've saved that" / "added to your memory" while allowing honest phrasings like "I haven't saved that" or "I'll keep this in mind"). **NOT done, and correctly so:** dropping `mustCallTool` from the gating pair — because neither case ever had a `mustCallTool` field (confirmed directly in `golden.json`: both were `null` before and after); the actual failing column was `mustContain`. More importantly, Brain's premise — "the fixtures carry `tier:free` in `userContext`, which the model legitimately sees" — does not hold: Coder captured the real outbound request and confirmed `tier:free`/`free tier`/`/upgrade` are all ABSENT from the pre-call surface; the only tier-adjacent string the model sees is PLAYER NOTES' `(Premium Account)`, which is Tibia's own in-game account status, unrelated to and pointing the opposite direction from TibiaEdge's subscription tier. **Independently reproduced and confirmed:** `playerContextService.ts`'s `buildUserContext` uses the `premium` boolean only for server-side branching (which sections to include) — the words "tier"/"free"/"premium" never get serialized into the rendered text sent to the model. So when the model wrote "not available on the free tier," it guessed with zero information and happened to be right because the fixture is free — the same guess on a real `pro` user would silently deny memory to a paying customer. `mustContain: "/upgrade"` is the only assertion that proves the dispatcher was actually consulted (the CTA text cannot appear any other way), and attempt 3 already proved the path is winnable, not structurally unwinnable — so it's catching a real intermittent defect, not a harness artifact. Kept as-is. Verified firsthand: full suite 704/704 green, `playerContextService.ts` independently reviewed and confirms Coder's claim exactly, `golden.json` confirmed to have never carried `mustCallTool` on either gating case.
